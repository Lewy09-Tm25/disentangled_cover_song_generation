"""
Training and inference toolkit for discrete token cover-song modeling.

Uses offline tensors produced by ``input_tensorization.py`` (do not rerun extraction).

See ``train.py`` and ``infer_eval.py`` for entry points.
"""
